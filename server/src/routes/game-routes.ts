import { Router } from 'express';

const router = Router();

// router.post('/', createNewGame);
// router.get('/:id', identifyGame, getGamePreview);
// router.get('/:id/state', identifyGame, getGameState);
// router.patch('/:id/state', identifyGame, patchGameState);

export default router;
